{
  "name": "lucidpanda-monorepo",
  "private": true,
  "packageManager": "pnpm@10.7.1",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "api:dev": "pnpm --filter api run dev",
    "web:dev": "pnpm --filter web run dev",
    "ios:generate": "pnpm --filter @lucidpanda/mobile-ios run generate",
    "generate-api": "pnpm --filter schema run generate"
  },
  "devDependencies": {
    "pnpm": "latest"
  }
}
